{ schema, values } = require('./values.coffee')
{ ExtComponentManager, ExtComponent } = require('../src/ext-component')

r = {
    options = {
        name: 'ComponentTest',
        action: 'define'
    },
    component = {
        extend: 'Ext.data.Model',
        fields: [

        ]
    }
}


describe('Testing basics of ext-component', () ->
    it('should return a config', () ->

    )
)